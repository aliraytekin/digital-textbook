import { IonHeader, IonToolbar, IonTitle } from "@ionic/react";

export default function Header() {
  return(
    <IonHeader>
      <IonToolbar>
        <IonTitle className="ion-title"> Digital Textbook Reader </IonTitle>
      </IonToolbar>
    </IonHeader>
  )
}
